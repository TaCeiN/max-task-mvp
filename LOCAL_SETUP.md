# Локальная разработка (без Docker)

Пошаговая инструкция по запуску проекта на вашем компьютере через терминал без использования Docker.

## Требования

Перед началом убедитесь, что у вас установлены:

1. **Python 3.11 или новее**
   - Проверка: `python --version` или `python3 --version`
   - Скачать: https://www.python.org/downloads/
   - ⚠️ При установке отметьте "Add Python to PATH"

2. **Node.js 18 или новее** и **npm**
   - Проверка: `node --version` и `npm --version`
   - Скачать: https://nodejs.org/
   - npm устанавливается автоматически вместе с Node.js

## Быстрый старт

### Шаг 1: Настройка Backend (Python/FastAPI)

Откройте **первый терминал** и выполните:

**Windows (PowerShell):**
```powershell
# Переход в папку backend
cd backend

# Создание виртуального окружения Python
python -m venv .venv

# Активация виртуального окружения
.venv\Scripts\Activate.ps1

# Если получили ошибку "execution policy", выполните сначала:
# Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Установка зависимостей
pip install -r requirements.txt

# Запуск backend сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Mac/Linux:**
```bash
# Переход в папку backend
cd backend

# Создание виртуального окружения Python
python3 -m venv .venv

# Активация виртуального окружения
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск backend сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Результат:** Вы должны увидеть:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

✅ **Оставьте этот терминал открытым!** Backend должен работать постоянно.

**Проверка:** Откройте в браузере http://localhost:8000/docs - должна открыться документация API (Swagger UI).

---

### Шаг 2: Настройка Webhook сервера (опционально, для получения webhooks от Max Bot)

Откройте **второй терминал** и выполните:

**Настройка переменных окружения:**

**Windows (PowerShell):**
```powershell
$env:MAX_BOT_TOKEN="your_bot_token_here"
$env:WEBHOOK_URL="https://webhook-devcore-max.cloudpub.ru"
```

**Windows (Command Prompt):**
```cmd
set MAX_BOT_TOKEN=your_bot_token_here
set WEBHOOK_URL=https://webhook-devcore-max.cloudpub.ru
```

**Linux/Mac:**
```bash
export MAX_BOT_TOKEN="your_bot_token_here"
export WEBHOOK_URL="https://webhook-devcore-max.cloudpub.ru"
```

**Запуск webhook сервера:**

**Windows:**
```powershell
cd backend
.venv\Scripts\Activate.ps1
python webhook_server.py
```

**Mac/Linux:**
```bash
cd backend
source .venv/bin/activate
python webhook_server.py
```

Или используйте готовые скрипты:

**Windows:**
```bash
cd backend
start_webhook_server.bat
```

**Linux/Mac:**
```bash
cd backend
chmod +x start_webhook_server.sh
./start_webhook_server.sh
```

**Результат:** Webhook сервер запустится на порту 8080 по умолчанию и автоматически подпишется на webhooks.

✅ **Оставьте этот терминал открытым!** Webhook сервер должен работать постоянно.

**Проверка:** Откройте в браузере http://localhost:8080/health - должен вернуться `{"status":"healthy"}`.

---

### Шаг 3: Настройка Frontend (React/Vite)

Откройте **третий терминал** (новое окно терминала) и выполните:

**Windows/Mac/Linux:**
```bash
# Переход в папку frontend
cd frontend

# Установка зависимостей Node.js
npm install

# Создание файла .env.local (опционально, по умолчанию используется http://localhost:8000)
echo VITE_API_URL=http://localhost:8000 > .env.local

# Запуск frontend сервера
npm run dev
```

**Результат:** Вы должны увидеть:
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

✅ **Оставьте этот терминал открытым!** Frontend должен работать постоянно.

---

## Открытие приложения

Откройте браузер и перейдите по адресу:

- **Frontend (основное приложение):** http://localhost:5173
- **Backend API (документация):** http://localhost:8000/docs
- **Webhook сервер:** http://localhost:8080/health

---

## Первое использование

### Автоматическая авторизация через URL параметры

Приложение поддерживает автоматическую авторизацию через URL параметры. Это основной способ входа в систему.

**Формат URL:**
```
http://localhost:5173/login?user_id=5107783&first_name=Иван&last_name=Иванов&username=ivan_ivanov
```

**Параметры:**
- `user_id` (обязательный) - уникальный идентификатор пользователя
- `first_name` (опционально) - имя пользователя
- `last_name` (опционально) - фамилия пользователя
- `username` (опционально) - имя пользователя (username)

**Как это работает:**
1. Откройте приложение с URL параметрами в браузере
2. Приложение автоматически извлечёт данные из URL
3. Отправит данные на backend для авторизации/регистрации
4. Если пользователь с таким `user_id` уже существует в базе - выполняется вход
5. Если пользователя нет - создаётся новый аккаунт
6. После успешной авторизации вы автоматически перенаправляетесь на главную страницу

---

## Остановка приложения

Чтобы остановить приложение:

1. В терминале с **frontend** нажмите `Ctrl+C` (Windows/Linux) или `Cmd+C` (Mac)
2. В терминале с **webhook сервером** нажмите `Ctrl+C` (Windows/Linux) или `Cmd+C` (Mac)
3. В терминале с **backend** нажмите `Ctrl+C` (Windows/Linux) или `Cmd+C` (Mac)
4. Деактивируйте виртуальное окружение Python (опционально):
   ```bash
   deactivate
   ```

---

## Настройка переменных окружения

### Backend

Создайте файл `.env` в папке `backend/` (опционально):

```env
DATABASE_URL=sqlite:///./data.sqlite3
SECRET_KEY=your_secret_key_here
ACCESS_TOKEN_EXPIRE_MINUTES=180
MAX_BOT_TOKEN=your_bot_token_here
```

### Webhook сервер

Переменные окружения для webhook сервера:

- `MAX_BOT_TOKEN` - токен бота (обязательно)
- `WEBHOOK_URL` - адрес webhook сервера для подписки (обязательно)
- `WEBHOOK_PORT` - порт для webhook сервера (по умолчанию: 8080)
- `WEBHOOK_HOST` - хост для webhook сервера (по умолчанию: 0.0.0.0)

### Frontend

Создайте файл `.env.local` в папке `frontend/` (опционально):

```env
VITE_API_URL=http://localhost:8000
```

По умолчанию в dev режиме используется `http://localhost:8000`.

---

## Команды для активации виртуального окружения

### Windows PowerShell:
```powershell
.venv\Scripts\Activate.ps1
```

### Windows Command Prompt (cmd):
```cmd
.venv\Scripts\activate.bat
```

### Mac/Linux:
```bash
source .venv/bin/activate
```

### Если получили ошибку "execution policy" в PowerShell (Windows):
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
Затем снова попробуйте активировать:
```powershell
.venv\Scripts\Activate.ps1
```

---

## Решение проблем

### Проблема: "Python не найден"
**Решение:** 
- **Windows:** Убедитесь, что Python установлен: `python --version`
- **Mac/Linux:** Попробуйте `python3 --version` (на Mac/Linux обычно используется `python3`)
- При установке Python (Windows) отметьте "Add Python to PATH"
- Перезапустите терминал после установки

### Проблема: "pip не найден"
**Решение:**

**Windows:**
```powershell
python -m ensurepip --upgrade
```

**Mac/Linux:**
```bash
python3 -m ensurepip --upgrade
```

### Проблема: "execution policy" при активации .venv (только Windows)
**Решение:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
**Примечание:** Эта проблема возникает только в Windows PowerShell. На Mac/Linux используйте `source .venv/bin/activate`.

### Проблема: "uvicorn не найден"
**Решение:** 
- Убедитесь, что виртуальное окружение активировано (должно быть `(.venv)` в начале строки)
- Переустановите зависимости:

**Windows:**
```powershell
pip install -r requirements.txt
```

**Mac/Linux:**
```bash
pip install -r requirements.txt
```

### Проблема: "npm не найден"
**Решение:** 
- Убедитесь, что Node.js установлен: `node --version`
- Переустановите Node.js с официального сайта: https://nodejs.org/
- Перезапустите терминал после установки

### Проблема: "Порт 8000 уже занят"
**Решение:** 
- Остановите другой процесс, использующий порт 8000
- Или измените порт backend:

**Windows/Mac/Linux:**
```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
```
- Не забудьте обновить `VITE_API_URL` в `.env.local` на `http://localhost:8001`

### Проблема: "Порт 8080 уже занят" (webhook сервер)
**Решение:**
- Остановите другой процесс, использующий порт 8080
- Или измените порт webhook сервера:

**Windows:**
```powershell
$env:WEBHOOK_PORT=9000
python webhook_server.py
```

**Mac/Linux:**
```bash
export WEBHOOK_PORT=9000
python webhook_server.py
```

### Проблема: "Порт 5173 уже занят"
**Решение:**
- Vite автоматически выберет другой порт (5174, 5175 и т.д.)
- Или остановите другой процесс, использующий порт 5173

### Проблема: "CORS ошибка" в браузере
**Решение:** 
- Убедитесь, что backend запущен и доступен по адресу `http://localhost:8000`
- Проверьте файл `.env.local` в frontend (должен содержать `VITE_API_URL=http://localhost:8000`)
- Откройте http://localhost:8000/docs в браузере - должна открыться документация API

### Проблема: "Модуль не найден" при запуске backend
**Решение:** 
1. Убедитесь, что виртуальное окружение активировано (должно быть `(.venv)` в начале строки)
2. Переустановите зависимости:

**Windows:**
```powershell
pip install -r requirements.txt
```

**Mac/Linux:**
```bash
pip install -r requirements.txt
```

### Проблема: "База данных не создаётся"
**Решение:** 
- Убедитесь, что у вас есть права на запись в папку `backend/`
- База данных `data.sqlite3` создаётся автоматически при первом запуске backend
- Проверьте, что backend запущен и нет ошибок в терминале

### Проблема: "Cannot connect to server" в frontend
**Решение:**
- Убедитесь, что backend запущен (проверьте первый терминал)
- Откройте http://localhost:8000/docs в браузере - должна открыться документация API
- Проверьте файл `.env.local` в frontend

### Проблема: "Webhook subscription failed"
**Решение:**
- Убедитесь, что `MAX_BOT_TOKEN` установлен
- Проверьте, что `WEBHOOK_URL` указан правильно
- Убедитесь, что webhook сервер запущен
- Проверьте логи webhook сервера

### Проблема: "Авторизация не работает через URL параметры"
**Решение:**
- Убедитесь, что URL параметры указаны правильно: `?user_id=...&first_name=...&last_name=...&username=...`
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что backend запущен и доступен
- Проверьте, что параметр `user_id` присутствует в URL (это обязательный параметр)

---

## Полезные команды

### Backend

**Windows:**
```powershell
# Активация виртуального окружения
.venv\Scripts\Activate.ps1

# Деактивация виртуального окружения
deactivate

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Сброс базы данных (если нужно)
python reset_db.py
```

**Mac/Linux:**
```bash
# Активация виртуального окружения
source .venv/bin/activate

# Деактивация виртуального окружения
deactivate

# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Сброс базы данных (если нужно)
python3 reset_db.py
```

### Webhook сервер

**Windows:**
```powershell
# Установка переменных окружения
$env:MAX_BOT_TOKEN="your_bot_token_here"
$env:WEBHOOK_URL="https://webhook-devcore-max.cloudpub.ru"

# Запуск webhook сервера
python webhook_server.py

# Или через скрипт
start_webhook_server.bat
```

**Mac/Linux:**
```bash
# Установка переменных окружения
export MAX_BOT_TOKEN="your_bot_token_here"
export WEBHOOK_URL="https://webhook-devcore-max.cloudpub.ru"

# Запуск webhook сервера
python webhook_server.py

# Или через скрипт
chmod +x start_webhook_server.sh
./start_webhook_server.sh
```

### Frontend

**Windows/Mac/Linux:**
```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр собранного проекта
npm run preview
```

---

## Проверка работы

### Backend
- Откройте http://localhost:8000/docs - должна открыться документация API (Swagger UI)
- Откройте http://localhost:8000/health - должен вернуться `{"status":"ok"}`

### Webhook сервер
- Откройте http://localhost:8080/health - должен вернуться `{"status":"healthy"}`
- Проверьте логи webhook сервера - должна быть информация о подписке на webhooks

### Frontend
- Откройте http://localhost:5173 - должна открыться страница приложения
- Откройте консоль браузера (F12) - не должно быть ошибок подключения к API

---

## Структура проекта

```
Test/
├── backend/                 # Backend (FastAPI)
│   ├── app/                # Код приложения
│   │   ├── main.py         # Точка входа
│   │   ├── routers/        # API роутеры
│   │   ├── models/         # Модели базы данных
│   │   └── ...
│   ├── data.sqlite3        # База данных (создаётся автоматически)
│   ├── requirements.txt    # Python зависимости
│   ├── webhook_server.py   # Webhook сервер
│   └── .venv/              # Виртуальное окружение Python
├── frontend/               # Frontend (React + Vite)
│   ├── src/                # Исходный код
│   │   ├── api/            # API клиент
│   │   ├── ui/             # UI компоненты
│   │   └── ...
│   ├── node_modules/       # Node.js зависимости
│   ├── package.json        # Node.js зависимости
│   └── .env.local          # Переменные окружения (создать вручную)
└── README.md               # Основной README
```

---

## Советы

1. **Три терминала:** Запускайте backend, webhook сервер и frontend в разных терминалах, чтобы видеть логи каждого сервера отдельно.

2. **Автоматическая перезагрузка:** Оба сервера поддерживают автоматическую перезагрузку при изменении кода:
   - Backend: `--reload` флаг в uvicorn
   - Frontend: автоматически в Vite

3. **База данных:** База данных SQLite создаётся автоматически при первом запуске backend в файле `backend/data.sqlite3`. Вы можете удалить этот файл для сброса базы данных.

4. **Переменные окружения:** Файлы `.env.local` в frontend и `.env` в backend опциональны - по умолчанию используются разумные значения. Но рекомендуется создать их для явной настройки.

5. **Авторизация через URL:** Приложение автоматически обрабатывает URL параметры для авторизации. Просто добавьте параметры `?user_id=...&first_name=...&last_name=...&username=...` к URL страницы логина.

6. **Webhook сервер:** Webhook сервер автоматически подписывается на webhooks при запуске. Убедитесь, что `MAX_BOT_TOKEN` и `WEBHOOK_URL` установлены правильно.

---

## Чеклист для первого запуска

- [ ] Python 3.11+ установлен и доступен в PATH
- [ ] Node.js 18+ и npm установлены
- [ ] Виртуальное окружение Python создано и активировано
- [ ] Зависимости backend установлены (`pip install -r requirements.txt`)
- [ ] Backend запущен на http://localhost:8000
- [ ] MAX_BOT_TOKEN установлен (для webhook сервера)
- [ ] WEBHOOK_URL установлен (для webhook сервера)
- [ ] Webhook сервер запущен на http://localhost:8080 (опционально)
- [ ] Зависимости frontend установлены (`npm install`)
- [ ] Файл `.env.local` создан в frontend (опционально)
- [ ] Frontend запущен на http://localhost:5173
- [ ] Приложение открыто в браузере и работает

---

Удачной разработки! 🚀
